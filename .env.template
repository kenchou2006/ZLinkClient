POSTGRES_URL=postgresql://username:password@localhost:5432/mydatabase
REDIS_URL = redis://localhost:6379
CACHE_TTL = None

# Cache-Control max-age/s-maxage (seconds) on redirect responses, for Vercel
# Edge Network caching. Defaults to 30 if unset. EDGE_CACHE_TTL_EXPIRING
# applies to links with an expiry set; defaults to EDGE_CACHE_TTL's value.
EDGE_CACHE_TTL = 30
EDGE_CACHE_TTL_EXPIRING = 30

GA_MEASUREMENT_ID = G-XXXXXXXXXX
GA_API_SECRET = XXXXXXXXXXXXXXXXXXXXXX